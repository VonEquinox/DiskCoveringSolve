# 十四圆盘覆盖：完整证明与精确重放包

主文：`PROOF_zh.md`。结论为

```text
0.331732034276234122557815548802409538270942754829017478062828
 < r_14 <
0.331732034276234122557815548802409538270942754829017478062829.
```

半径的精确定义不是这个有限小数区间，而是 `root_certificate.json` 中有理隔离盒内116元整系数二次系统的唯一根的平方根。

## 执行

```bash
cd r14_verified_proof
python3 -B verify_all.py
```

要求：Python标准库、PATH中的`g++`（支持C++17）、Boost.Multiprecision头文件。无需网络、NumPy、SciPy、商业优化器或GUI。实际发现环境版本列在`ENVIRONMENT.json`。不要关闭Python断言；`-O`、`-OO`会被拒绝。

总验证器从头重算所有接受条件，重新编译轨道审计器，把详细日志写到`replay_logs/`，把最终报告写到`r14_MASTER_VERIFIED.json`。归档中的旧报告只是本次交付的运行记录，不是下一次重放的输入。失败会写入`verified:false`并非零退出。

## 证明闭合的计数

- 四族全轨道：1,313,024。
- 直接角矛盾和Farkas排除：1,305,792。
- 非候选残余：7,231个，11,621节点，9,426个严格整数力排除叶。
- 候选：599节点，266个严格力排除叶，34个完整落入锚点隔离域的局部叶。
- 未决分支：0。

每次重放检查这些部分之间的面集、索引、文件哈希与集合覆盖。正文解释为什么任意严格更小的圆盘覆盖都必须进入这个有限域，不把枚举成功等同于几何归约已经证明。

## 上界中不能忽略的细节

候选中心三角剖分有16个面：8个正应力面、6个精确紧但零应力的面、2个严格松弛面。六个紧面用书面证明及精确结构检查给出的共同点恒等式处理。

另外两个径向面精确退化，已删去，并把相应圆心当作多边形弦上的中间边界顶点。实际验证的是24顶点、57边、34个正向三角面的覆盖链，而不是错误地要求36个三角形全部严格正向。

## 可信范围

这是书面几何与组合论证加精确机器证书的计算机辅助证明。没有经过第三方同行评审，也没有Lean/Coq形式化。验证只使用整数、有理数、显式级数余项及整数平方根；`discovery/`中的优化器输出不参与接受。
